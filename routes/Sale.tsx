const Sale = () => {
  return (
    <section className="flex items-center justify-center">
      <div>
        <button className="btn btn-lg">MSR or Out</button>
        <button className="btn btn-lg">New</button>
      </div>
    </section>
  );
};

export default Sale;
